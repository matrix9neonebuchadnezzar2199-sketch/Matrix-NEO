"""MATRIX-NEO FastAPI application package."""

# Single source of truth for release version (run_server / PyInstaller use the same app).
# TODO: optionally wire importlib.metadata.version when distributed as an installable package.
__version__ = "3.2.0"
